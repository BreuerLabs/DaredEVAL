name: "drop_layer"
penalty: "lasso" # can set this to False for no penalty
apply_threshold: True
lasso:
  lambda_: 0.1
  threshold: 0.01
  smooth: False
  alpha: 0.04 # smoothing hyperparameter, see Zhang et al. 2020
  
input_defense_init: 0.5 # needs to be a float I think
plot_mask: True
plot_save_name: "test_mask"