# conf/config.yaml
defaults:
  - model: plug_and_play   # Yaml file describing which model and hyperparameters to use
  - dataset: CelebA  # Yaml file with dateset specific parameters
  - training: default # Yaml file with training specific parameters

target_wandb_id: "s7t7p479"
target_config_path: False #"classifiers/saved_configs/cifardropout.yaml" 